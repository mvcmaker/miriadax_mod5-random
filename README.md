# Random: 
> **Este programa debe ser ejecutado con nodejs**
```bash
$ node random.js
```
 * Generador de números aleatorios. Cada vez que lo ejecutas, se genera un número aleatorio de 0 a 1 y se informa por consola si el resultado es mayor o menor del valor 0.5